spring:
  profiles:
    active:
      on-profile: dev
  devtools:
    add-properties: true
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/sp2}
    username: ${POSTGRE_USER:postgres}
    password: ${POSTGRE_PASSWORD:292004nhan}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show_sql: false
    hibernate:
      ddl-auto: none


server:
  port: 80

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

openapi:
  service:
    title: Sp2 document
    version: v1.0.0
    server: http://localhost:${server.port}
